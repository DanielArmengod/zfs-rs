### zfs-rs replicate tank/webdata backup/webdata ###

build cmdline 1
	zfs send --compressed --embed --large-block --props [--verbose] -i @MOST_RECENT_SOURCE_SNAPSHOT SOURCE_DATASET@MOST_RECENT_COMMON_SNAPSHOT
	
build cmdline 2 -> 
    zfs recv [--opts...] DESTINATION_DATASET
    








Lisf of things to do:
    * Implement comm
    * Implement full send
    * Improve the order of operations in replicate, particularly:
        * Take the snapshot (-t) only after executing find_mrcud(), if it is determined that
            SourceHasMore, or
            UpToDate, or
            opts.allow_divergent_destination and
                DestinationHasMore, or
                Divergence
    * Implement spinny-twirly :emoji: progress bars
    * Rework manual
    * Rework clap parsing
    * Update dependencies